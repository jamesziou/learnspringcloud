spring:
  application:
    name: license-service
  profiles:
    active: default
  cloud:
    config:
      uri: http://localhost:8081/config-service
      enabled: true
  config:
    import:
server:
  port: 8080
  servlet:
    context-path: /license-service